#!/usr/local/bin/python3
import sys
#print(dir(sys))
#print(help(sys))
print(sys.version)
print(sys.version_info)
print(sys.path) #sys.path is an environmental veriable for python

#by default sys.exit() will provide return code 0 and if we want any other return code write like this sys.exit(2)
print("python")
sys.exit()
